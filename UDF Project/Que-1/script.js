function add() {
    first = parseInt(calc.first.value);
    sec = parseInt(calc.sec.value);
    thrd = first + sec;
    calc.thrd.value = thrd;
}

function minus() {
    first = parseInt(calc.first.value);
    sec = parseInt(calc.sec.value);
    thrd = first - sec;
    calc.thrd.value = thrd;
}

function multy() {
    first = parseInt(calc.first.value);
    sec = parseInt(calc.sec.value);
    thrd = first * sec;
    calc.thrd.value = thrd;
}

function division() {
    first = parseInt(calc.first.value);
    sec = parseInt(calc.sec.value);
    thrd = first / sec;
    calc.thrd.value = thrd;
}a